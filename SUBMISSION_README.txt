The file kmdb.sql contains all the SQL code for the assignment submission. 
However, we still need to access the SQL environment in the console. Before reading the kmdb.sql file please prompt the terminal with the code below:

sqlite3 hw1.sqlite3
.read kmdb.sql

END